# 我的需求

## 进一步细节的修改

现在整个页面有几个问题需要进行修改：

1. Search 页面
   如果搜索框里面并没有写入任何文字，那么请不要显示搜索结果。也就是说，如果搜索框里没有东西，就不应该显示下面的文章链接。只有当我在搜索框里开始键入内容时，才应该显示匹配到的文章链接。

2. 日历页面
   年份和月份这两个地方应该做成可以点击的状态，点击后弹出一个可以上下滑动的组件，让我可以快速地用滑轮的方式选择对应的年份和月份。整体的实现效果应该参考 iOS 的官方实现，即 iOS 应用中快速选择时间日期的交互方式。你在这里需要给我提供类似的选项。 参考我的截图。

2. 日历页面你还需要加一个按钮，加一个字“NOW”，它可以快速地返回当日。

这个位置你就放在 Calendar 那一行的右边，小小的一个“NOW”，我点一下就可以快速返回当日。

另外把“图片链接”这个功能给删除掉。

因为实际上，我之后所有的图片都会以“相册选择”的方式插进去，而不是以“图片链接”的方式插进去，所以你就直接把“图片链接”这个功能给删掉。

Journal 页面的顶部日期，在点击之后，应该可以快捷地回到当天。然后在 Journal 页面需要做一个左右滑动的功能，可以快速切换前天和后天。

然后这些文章的块，你应该可以点击。点击打开之后，它应当可以在一个独立的页面。你可以在这里面去读你的图片和正文，你应该把它做成一个文章阅读页的样子。应当在文章阅读页进行编辑。

1. 进入编辑模式：
   点击右上角的“编辑”按钮即可进入。
2. 按钮功能：
   进入编辑模式后，左上角和右上角的按钮分别是“取消”和“保存”。
3. 阅读模式：
   不进入编辑模式时，页面就是一个纯粹的阅读界面

那这样的话，你就不需要在 Journal、Blog 或者 Search 里面，针对文章展示块的右下角去放一个小设置按钮了。直接把那个小按钮给干掉。

接下来的操作逻辑是：
1. 我点击这篇文章，进入阅读模式。
2. 点击右上角的“编辑”，进入编辑模式。
3. 在编辑模式下，你应该给我加一个删除文章内容的按钮，方便我直接把它给删掉。

当然，整个文章的这个块，你应该有一个更加符合直觉的删除方法。

就是我在文章块上向左滑（滑动的同时会出现“编辑”和“删除”这两个按钮），我向左边滑，它就边出现编辑和删除按钮。

1. 点击“编辑”：直接进入编辑模式。
2. 点击“删除”：直接弹出一个对话框确认删除，然后就把这个文档给删了。

请你把打开应用后进入的测试文章给删掉，只保留一个测试文章。

这一个测试文章的要求如下：
1. 内容：告诉用户该怎么用这个应用。
2. 图片：从网上随便下一个图片，或者搞一个占位图片就行。

总之就是把现在已有的这些测试文章全部删掉。

另外，Blog 页面的“添加文章”按钮应该和 Journal 页面保持一致：

1. 在右下角放置一个蓝色的加号
2. 去掉右上角的加号按钮


## 概述


我已经快速实现了一个网页的原型 lumina，请你将其作为 UI 参考。

## 基本功能

我想要实现的功能如下：

1. Journal 页面
   (a) 页面正上方默认显示当天的日期。
   (b) 页面主体按照年份显示一系列小框，每个框包含图片和文字。
   (c) 框的下方显示星期几。
   (d) 核心逻辑：将每一年的同一天所创建的文章（或带图的文章）显示在同一个位置。
   (e) 点击页面左上方的日历图标，跳转至日历页面。

2. 日历页面
   (a) 这是一个标准的日历组件，支持年份和月份的显示。
   (b) 点击日历中的某一日期，即可跳转到当天的 Journal 内容。

3. Search 页面
   (a) 用户输入搜索关键词，系统迅速检索并显示相关的文章。

4. Blog 页面
   (a) 该页面需要一个添加按钮，用于创建新文章。
   (b) 此处添加的文章与前面的 Journal 文章无关，是单独的内容。
   (c) 这些文章不会出现在 Journal 或 Reflect 中，但会出现在 Search 的搜索结果中。
   (d) 页面按照时间流排列，目前每篇文章仅限于一张图片和一大段文字。

请根据以上 UI 逻辑开发一个 iOS App。要求编写完整的测试代码。

## 联网功能

需要有一个联网功能。整个页面应该再找一个合适的地方，让我能够放设置页面。

设置页面的布局要求如下：
1. 放在第一页的右上角
2. 把原先右上角的“搜索”改为“设置”

关于设置页面的功能逻辑：
1. 可以通过 CloudKit 的功能，把整个存储仓库分享出去
2. 分享方式：通过邀请链接分享给别人
3. 邀请逻辑：请参考我已经实现的另外一个项目 /Users/wangyu/code/homeLibraryApp/homeLibrary
4. 注意，作为仓库的所有者，我可以设定在邀请他人时，对方不能修改当前仓库的任何内容，只能查看。当然也可以给他修改权限，这全都是我在这里设置的。

## 注意

注意：如果当前的 UI 有哪里不合理的地方，请进行修改。

因为这只是我的一个实现原型，并不一定最终所有的功能都测试到了，你需要按照功能要求去修改这个 UI。


注意到在这个过程中，你需要记录 log.md 文件。也就是说，当你完成一个比较大的、跨越性的功能实现时，就要记录在 log 里面。

同时，你也要参考我在需求中提到的/Users/wangyu/code/homeLibraryApp/homeLibrary，给我写一份 README 文件。